"use client";

/** STUB from the integrator — W3 replaces this file, keeping the export name and props. */
import type { VettuOverlayProps } from "@/lib/contracts/overlay";

export function VettuOverlay(_props: VettuOverlayProps) {
  return null;
}
